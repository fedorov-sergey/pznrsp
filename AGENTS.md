# Правила проекта

## Общее

1. Это сайт, созданный через GitHub. Домен — **analizsmet.ru**.
2. Сайт — инструкции и покупка подписки приложения «Анализ смет».
3. Приложение анализирует файлы ГРАНД-Сметы (.gsfx), выводит отчёт-таблицу настроек/данных. Первые 5 дней бесплатно, затем — подписка.
4. Все страницы сайта — визуальное продолжение друг друга.
5. Все страницы должны хорошо читаться как на компьютере, так и на смартфоне.
6. Общение с пользователем и рассуждения — на **русском языке**.
7. Комментарии в коде — на **русском языке**.

## Стек и технологии

8. Фреймворк — **Bootstrap 5** (CDN), иконки — **Bootstrap Icons** (CDN). Не подключать сторонние CSS/JS-библиотеки без необходимости.
9. Все стили — в `<style>` внутри `<head>` конкретной страницы. Глобальный CSS-файл не используется.
10. Подход к верстке — **блочный** (как в Тильде): каждая секция страницы — это отдельный `<section>` с собственным фоном/отступом. Это обеспечивает визуальную целостность и адаптивность.

## Структура каждой страницы (каркас)

Каждая новая страница должна повторять этот каркас:

```
1. <nav>         — тёмный navbar (bg-dark), логотип «Анализ смет», меню-бургер на мобильных, offcanvas-menu
2. <section>     — hero/header — градиентный заголовок (linear-gradient #0d6efd -> #0a58ca)
3. <section>     — основной контент (py-5, белый или серый фон, чередование)
4. <footer>      — тёмный футер (bg-dark, серый текст), ссылка «На главную»
```

- Навигация и футер — **идентичны** на всех страницах.
- При добавлении новой страницы — добавлять пункт в navbar (и обычное меню, и offcanvas-меню).
- Hero-секция обязательна для заглавных страниц. Для внутренних — допустим заголовок без градиента.

## Навигация (текущее меню)

```
Главная -> index.html
Помощь -> videos.html
Тарифы -> rate.html
```

При добавлении новой страницы — добавлять `<li class="nav-item">` в оба места: обычное меню и offcanvas-меню.

## Адаптивность

11. Использовать Bootstrap-классы: `col-lg-*`, `col-md-*`, `d-flex`, `d-grid`, `d-md-flex`, `w-100`, `gap-*`.
12. На мобильных — вертикальная раскладка (`d-grid gap-2`), на десктопе — горизонтальная (`d-md-flex`).
13. Таблицы на десктопе -> карточки на мобильных (скрывать/показывать через `@media`).

## Соцсети и мессенджеры

Повторяющиеся ссылки (использовать везде одинаково):

```
Telegram:   https://t.me/pznrsp
ВКонтакте:  https://vk.com/pznrsp
Макс:       https://max.buzz/i/pznrsp
YouTube:    https://www.youtube.com/@pznrsp
ВК Видео:   https://vkvideo.ru/@pznrsp
Дзен:       https://dzen.ru/pznrsp
RuTube:     https://rutube.ru/channel/8968994
```

## SEO

14. У каждой страницы должны быть заполнены `<title>` и `<meta name="description">`.
15. При создании или удалении страницы — обновлять `sitemap.xml` (добавлять/убирать URL).
16. Страницы с описанием парсеров — ключевые для индексации.

## Приложение (справочная информация для контента)

- Название: «Анализ смет»
- Формат файлов: ГРАНД-Смета (.gsfx)
- Платформы: Windows, Linux (Astra, Alt, RED OS и др.)
- Модель: 5 дней бесплатно, затем — подписка через Boosty
- Ключевая функция: пакетный анализ множества сметных файлов, вывод сравнительной таблицы настроек/данных для быстрого поиска ошибок
